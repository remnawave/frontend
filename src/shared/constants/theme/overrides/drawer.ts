import { DrawerOverlay } from '@mantine/core'

export default {
    DrawerOverlay: DrawerOverlay.extend({
        defaultProps: {
            backgroundOpacity: 0.6,
            blur: 0
        }
    })
}
